# vim:sw=2:ts=2
name: ci

# on events
on:
  push:
    branches:
      - master
      - 'bpaczek/*'
  pull_request:
    branches:
      - master
      - 'feature/*'


jobs:
  call-ci:
    uses: bartekpaczek/phoenix-rtos-project-fork/.github/workflows/ci-submodule-misra.yml@bpaczek/misra-ci
    secrets: inherit
    with:
      build_params: all tests
